export const SET_HOLDINGS = (state, holdings) => {
  state.holdings = holdings
}

export const SET_DATA_CONFIG = (state, dataConfig) => {
  state.dataConfig = dataConfig
}

export const SET_XRAY_DATA = (state, xrayData) => {
  state.xrayData = xrayData
}
